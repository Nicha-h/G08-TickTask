export interface Task {
    id: number;
    title: string;
    completed: boolean;
  }
export type email = string;
export type plainPassword = string;

export type User = {
    id: number;
    email: string;
    password: string;
  };
  
  